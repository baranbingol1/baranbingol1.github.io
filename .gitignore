*.swp
~*
*~
_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
_notebooks/.ipynb_checkpoints

# Local preview only — GitHub Pages resolves its own gem versions and ignores
# both of these on the classic build.
.bundle
Gemfile.lock
